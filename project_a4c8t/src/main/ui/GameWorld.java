package ui;

// Represent a gameWorld containing the whole game process

import model.Event;
import model.EventLog;

public class GameWorld {

    // Construct a gameWord and call the start panel
    public GameWorld() {
        StartPanel startPanel = new StartPanel();


    }
}
